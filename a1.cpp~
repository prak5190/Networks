#include <iostream>
#include <cstdio>

using namespace std; 

// Parse string and return true false and position if false
void parseUsingArray(char* str) {
  while (*str != '\0') {
    cout<<"Strin"<<*str;
  }
}


int main (int argc ,char** argv) {
  parseUsingArray("helloWorld");
  return 0;
}
